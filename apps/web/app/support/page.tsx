import { PublicDocument } from "../../components/public-document";
export default function SupportPage() { const email = process.env.NEXT_PUBLIC_SUPPORT_EMAIL ?? "support@yadraw.com"; return <PublicDocument title="Support"><h2>Contact</h2><p>Email <a href={`mailto:${email}`}>{email}</a> with the affected board URL, time of the issue, and a short description. Do not send passwords, access tokens, or private file contents.</p><h2>Account access</h2><p>Use Forgot password on the sign-in page first. For account deletion or privacy requests, write from the email address associated with your account.</p></PublicDocument>; }
